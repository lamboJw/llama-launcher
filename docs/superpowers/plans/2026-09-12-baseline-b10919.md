# llama.cpp 基线升至 b10919 + 补齐新增参数 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** launcher 托管基线从 b10488 升到 b10919，表单补齐区间内新增的 5 个常用 server 参数（lazy-mode / n-cpu-ffn / mmproj-device / kv-unified-per-slot / log-jsonl）。

**Architecture:** 纯增量改动，不引入新模块。基线常量 + 文案同步（version.ts / renderer）；新字段走现有「四处同步」模式（types.ts FormValues → config.ts DEFAULT_FORM → args.ts buildArgs → renderer GROUPS），复用 str()/bool() 组装辅助；测试沿用 vitest 纯 Node 单测。

**Tech Stack:** TypeScript ESM（tsc 直出）、vitest、Electron renderer（无打包器）。

**规格:** `docs/superpowers/specs/2026-09-12-baseline-b10919-design.md`

**提交约定:** `type: 中文描述`（feat/fix/chore/docs）。

**文件结构（全部为修改，无新建）：**

| 文件 | 职责/改动 |
|---|---|
| `src/main/version.ts` | `BASELINE_BUILD` 10488 → 10919 |
| `src/renderer/main.ts` | `fillExeOptions` 基线文案；`GROUPS` 新增 5 字段 + loadMode label |
| `src/shared/types.ts` | `FormValues` 新增 5 字段 |
| `src/main/config.ts` | `DEFAULT_FORM` 新增 5 字段默认值 |
| `src/main/args.ts` | `buildArgs` 透传 5 个新参数 |
| `test/version.test.ts` | 基线断言 + b10919 样本 |
| `test/args.test.ts` | `BASE` 补字段 + 5 个新参数断言 |
| `docs/superpowers/specs/2026-07-25-llama-server-launcher-design.md` | 基线表述 + §5 参数表 |

**刻意不改：** `src/main/updater.ts`（b10919 x64 资产命名已验证兼容）、`config.ts` 的 `migrateForm`（JsonStore 的 `{...defaults, ...parsed}` 合并自动补新字段）、preload、proxy。

**环境注意:** vitest 配置了 `fileParallelism: false`，单文件用 `npx vitest run test/xxx.test.ts`；`npm run typecheck` = `tsc --noEmit`（只含 `src/`，测试文件不参与类型检查，但改类型后仍要跑以验证 src 一致）。

---

### Task 1: 基线常量与版本测试

**Files:**
- Modify: `test/version.test.ts`
- Modify: `src/main/version.ts:5`

- [ ] **Step 1: 改测试（先失败）**

`test/version.test.ts` 共 5 处编辑：

1. 第 5 行 `const NEW_OUT = ...` 之后新增常量（版本号前缀正则不校验，build/commit 必须准确）：

```ts
const NEW10919_OUT = 'version: 0.4.0 (build 10919, commit d3146f2b5)\nbuilt with Clang 20.1.8 for Windows x86_64\n';
```

2. `parses new format (b10488 real output)` 测试块（L15-19）之后新增：

```ts
  it('parses b10919 output', () => {
    const v = parseVersion(NEW10919_OUT);
    expect(v.build).toBe(10919);
    expect(v.commit).toBe('d3146f2b5');
  });
```

3. `baseline -> no banner`（L31）：`parseVersion(NEW_OUT)` → `parseVersion(NEW10919_OUT)`

4. `non-baseline -> banner`（L38）：`expect(b!).toContain('b10488');` → `expect(b!).toContain('b10919');`

5. L89：`expect(BASELINE_BUILD).toBe(10488);` → `expect(BASELINE_BUILD).toBe(10919);`

（保留 L15-19 的 b10488 真实输出解析用例不动——它验证 NEW_RE 格式。）

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/version.test.ts`
Expected: FAIL——`baseline -> no banner`、`non-baseline -> banner with version and baseline`、`baseline constant` 三个用例失败（BASELINE_BUILD 仍是 10488）；`parses b10919 output` 通过（解析器与版本无关）。

- [ ] **Step 3: 改实现**

`src/main/version.ts:5`：

```ts
export const BASELINE_BUILD = 10919;
```

（`versionBanner` 与 `resolveExe` 错误文案经 `b${BASELINE_BUILD}` 模板自动更新，无需改。）

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/version.test.ts`
Expected: PASS（全部用例）。

- [ ] **Step 5: 提交**

```bash
git add test/version.test.ts src/main/version.ts
git commit -m "feat: llama.cpp 基线升至 b10919（版本解析与横幅）"
```

---

### Task 2: renderer 版本下拉基线文案

**Files:**
- Modify: `src/renderer/main.ts:300-301`（`fillExeOptions`）

- [ ] **Step 1: 改代码**

```ts
  const hasBaseline = installed.some((v) => v.tag === 'b10919' && v.valid !== false);
  o0.textContent = hasBaseline ? '默认（托管基线 b10919）' : '默认（托管基线 b10919，未安装）';
```

（renderer 无单测覆盖，靠 typecheck + 后续手动验证。）

- [ ] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: 无输出、退出码 0。

- [ ] **Step 3: 提交**

```bash
git add src/renderer/main.ts
git commit -m "feat: 版本下拉基线文案同步 b10919"
```

---

### Task 3: FormValues 与 DEFAULT_FORM 新增 5 字段

**Files:**
- Modify: `src/shared/types.ts:29-67`（FormValues）
- Modify: `src/main/config.ts:10-47`（DEFAULT_FORM）

- [ ] **Step 1: types.ts 四处插入**

- 模型组（L32）：

```ts
  mmprojAuto: boolean; mmprojOffload: boolean; mmprojDevice: string;
```

- 硬件组（L40-41）：

```ts
  device: string; loadMode: string; lazyMode: string; fit: string; tensorSplit: string;
  cacheTypeK: string; cacheTypeV: string; nCpuMoE: string; nCpuFfn: string;
```

- 上下文组（L45）：

```ts
  cacheRam: string; flashAttn: string; kvUnified: string; kvUnifiedPerSlot: string; swaFull: boolean;
```

- 高级组（L61）：

```ts
  perf: boolean; logJsonl: boolean; logPromptsDir: string; mcpServersConfig: string;
```

- [ ] **Step 2: 先只跑 typecheck 确认失败（DEFAULT_FORM 缺新字段）**

Run: `npm run typecheck`
Expected: FAIL——`DEFAULT_FORM` 报缺 `mmprojDevice`/`lazyMode`/`nCpuFfn`/`kvUnifiedPerSlot`/`logJsonl`。

- [ ] **Step 3: config.ts DEFAULT_FORM 四处插入**

- 模型组（L13）：

```ts
  mmprojAuto: true, mmprojOffload: true, mmprojDevice: '',
```

- 硬件组（L21-22）：

```ts
  device: '', loadMode: '', lazyMode: '', fit: '', tensorSplit: '',
  cacheTypeK: '', cacheTypeV: '', nCpuMoE: '', nCpuFfn: '',
```

- 上下文组（L25）：

```ts
  cacheRam: '', flashAttn: '', kvUnified: '', kvUnifiedPerSlot: '', swaFull: false,
```

- 高级组（L41）：

```ts
  perf: false, logJsonl: false, logPromptsDir: '', mcpServersConfig: '',
```

- [ ] **Step 4: 类型检查通过**

Run: `npm run typecheck`
Expected: 无输出、退出码 0。

- [ ] **Step 5: 提交**

```bash
git add src/shared/types.ts src/main/config.ts
git commit -m "feat: FormValues 新增 b10919 五字段（lazyMode/nCpuFfn/mmprojDevice/kvUnifiedPerSlot/logJsonl）"
```

---

### Task 4: buildArgs 透传 5 个新参数

**Files:**
- Modify: `test/args.test.ts`（BASE 常量 + 新增测试）
- Modify: `src/main/args.ts`（buildArgs）

- [ ] **Step 1: 测试 BASE 常量补 5 字段**

`test/args.test.ts` BASE（L5-29）四处编辑：

- L7：`mmprojAuto: true, mmprojOffload: true, imageMinTokens: '', imageMaxTokens: '',` →
  `mmprojAuto: true, mmprojOffload: true, mmprojDevice: '', imageMinTokens: '', imageMaxTokens: '',`
- L12：`device: '', loadMode: '', fit: '', tensorSplit: '', cacheTypeK: '', cacheTypeV: '', nCpuMoE: '',` →
  `device: '', loadMode: '', lazyMode: '', fit: '', tensorSplit: '', cacheTypeK: '', cacheTypeV: '', nCpuMoE: '', nCpuFfn: '',`
- L14：`cacheRam: '', flashAttn: '', kvUnified: '', swaFull: false,` →
  `cacheRam: '', flashAttn: '', kvUnified: '', kvUnifiedPerSlot: '', swaFull: false,`
- L25：`perf: false, logPromptsDir: '', mcpServersConfig: '',` →
  `perf: false, logJsonl: false, logPromptsDir: '', mcpServersConfig: '',`

（BASE 里缺 `logJsonl` 时 `bool()` 的 `typeof v === 'boolean'` 不成立 → 不传，off 用例会假失败，必须先补。）

- [ ] **Step 2: 新增 5 个测试**

`describe('buildArgs')` 内、`empty string fields are not passed` 测试（L75-80）之后插入：

```ts
  it('lazyMode → --lazy-mode N（空 → 不传）', () => {
    expect(hasPair(buildArgs({ ...BASE, lazyMode: 'on' }, LOCAL, 59999).argv, '--lazy-mode', 'on')).toBe(true);
    expect(hasPair(buildArgs({ ...BASE, lazyMode: 'auto' }, LOCAL, 59999).argv, '--lazy-mode', 'auto')).toBe(true);
    expect(hasPair(buildArgs({ ...BASE, lazyMode: 'off' }, LOCAL, 59999).argv, '--lazy-mode', 'off')).toBe(true);
    expect(buildArgs(BASE, LOCAL, 59999).argv).not.toContain('--lazy-mode');
  });

  it('nCpuFfn → --n-cpu-ffn N（空 → 不传）', () => {
    expect(hasPair(buildArgs({ ...BASE, nCpuFfn: '3' }, LOCAL, 59999).argv, '--n-cpu-ffn', '3')).toBe(true);
    expect(buildArgs(BASE, LOCAL, 59999).argv).not.toContain('--n-cpu-ffn');
  });

  it('mmprojDevice → --mmproj-device（none/设备名透传，空 → 不传）', () => {
    expect(hasPair(buildArgs({ ...BASE, mmprojDevice: 'none' }, LOCAL, 59999).argv, '--mmproj-device', 'none')).toBe(true);
    expect(hasPair(buildArgs({ ...BASE, mmprojDevice: 'cuda1' }, LOCAL, 59999).argv, '--mmproj-device', 'cuda1')).toBe(true);
    expect(buildArgs(BASE, LOCAL, 59999).argv).not.toContain('--mmproj-device');
  });

  it('kvUnifiedPerSlot → --kv-unified-per-slot N（空 → 不传）', () => {
    expect(hasPair(buildArgs({ ...BASE, kvUnifiedPerSlot: '4096' }, LOCAL, 59999).argv, '--kv-unified-per-slot', '4096')).toBe(true);
    expect(buildArgs(BASE, LOCAL, 59999).argv).not.toContain('--kv-unified-per-slot');
  });

  it('logJsonl → --log-jsonl / --no-log-jsonl 恒显式成对', () => {
    const on = buildArgs({ ...BASE, logJsonl: true }, LOCAL, 59999);
    expect(on.argv).toContain('--log-jsonl');
    expect(on.argv).not.toContain('--no-log-jsonl');
    const off = buildArgs(BASE, LOCAL, 59999);
    expect(off.argv).toContain('--no-log-jsonl');
    expect(off.argv).not.toContain('--log-jsonl');
    expect(off.argToField['--log-jsonl']).toBe('logJsonl');
    expect(off.argToField['--no-log-jsonl']).toBe('logJsonl');
  });
```

- [ ] **Step 3: 运行测试确认失败**

Run: `npx vitest run test/args.test.ts`
Expected: FAIL——5 个新用例全失败（buildArgs 尚未透传）。

- [ ] **Step 4: 实现 args.ts 五处插入**

- 模型组，`bool('mmprojOffload', '--mmproj-offload');`（L91）之后：

```ts
  str('mmprojDevice', '--mmproj-device');
```

- 硬件组，`str('loadMode', '--load-mode');`（L109）之后：

```ts
  str('lazyMode', '--lazy-mode');
```

- 硬件组，`str('nCpuMoE', '--n-cpu-moe');`（L113）之后：

```ts
  str('nCpuFfn', '--n-cpu-ffn');
```

- 上下文组，kvUnified if/else 块（L124-125）之后：

```ts
  str('kvUnifiedPerSlot', '--kv-unified-per-slot');
```

- 高级组，`bool('perf', '--perf');`（L173）之后：

```ts
  bool('logJsonl', '--log-jsonl');
```

（`bool()` 自动生成 `--no-log-jsonl` 变体并登记两个 argToField 映射。）

- [ ] **Step 5: 运行测试确认通过**

Run: `npx vitest run test/args.test.ts`
Expected: PASS（含新增 5 用例）。

- [ ] **Step 6: 提交**

```bash
git add test/args.test.ts src/main/args.ts
git commit -m "feat: buildArgs 透传 b10919 五个新参数"
```

---

### Task 5: renderer 表单组新增 5 字段 + loadMode label

**Files:**
- Modify: `src/renderer/main.ts:46-139`（GROUPS）、`:76`（loadMode label）

- [ ] **Step 1: GROUPS 五处插入 + label 修改**

- 模型组，`{ id: 'mmprojOffload', ... }`（L52）之后：

```ts
    { id: 'mmprojDevice', label: 'mmproj 设备 (mmproj-device，none=不放 GPU)', type: 'text' },
```

- 硬件组，L76 改为两行：

```ts
    { id: 'loadMode', label: '内存加载 (load-mode)', type: 'text' },
    { id: 'lazyMode', label: '内存按需加载 (lazy-mode)', type: 'select', options: [['', '默认(不传)'], ['auto', 'auto'], ['on', 'on'], ['off', 'off']] },
```

- 硬件组，`{ id: 'nCpuMoE', ... }`（L80）之后：

```ts
    { id: 'nCpuFfn', label: 'CPU FFN 层数 (n-cpu-ffn)', type: 'text' },
```

- 上下文组，`{ id: 'kvUnified', ... }`（L90）之后：

```ts
    { id: 'kvUnifiedPerSlot', label: '每 slot 上下文上限 (kv-unified-per-slot，与 ctx-size 同设时不生效)', type: 'text' },
```

- 高级组，`{ id: 'perf', ... }`（L132）之后：

```ts
    { id: 'logJsonl', label: 'JSONL 日志 (log-jsonl)', type: 'checkbox' },
```

（`FieldSpec.id: keyof FormValues` 由 typecheck 保证字段名与类型一致；select 空值选项与 fit/kvUnified 同模式，留空 = 不传。）

- [ ] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: 无输出、退出码 0。

- [ ] **Step 3: 提交**

```bash
git add src/renderer/main.ts
git commit -m "feat: 表单新增 5 个 b10919 字段并更新 load-mode label"
```

---

### Task 6: 主规格基线表述与 §5 参数表更新

**Files:**
- Modify: `docs/superpowers/specs/2026-07-25-llama-server-launcher-design.md`

- [ ] **Step 1: 基线表述（4 处）**

- L12-13：`当前已下载 b10488 / build 10488 / commit 9d77fa172，` → `当前基线 b10919 / build 10919 / commit d3146f2b5，`
- L160：`按 llama-server **b10488**（build 10488，commit 9d77fa172，表单基线版本）实际 --help 参数分组。` → `按 llama-server **b10919**（build 10919，commit d3146f2b5，表单基线版本）实际 --help 参数分组。`
- L302：`表单内置**基线版本 b10488**（参数集的设计依据）。` → `表单内置**基线版本 b10919**（参数集的设计依据）。`
- L303：`参数表单基于 b10488 设计，` → `参数表单基于 b10919 设计，`

（历史实测示例保持原文不动：L130/142/300-301（两种格式示例）/330/341/345/362。）

- [ ] **Step 2: §5 参数表（L169-177）补 5 个参数**

- 模型行（L171）：`--mmproj-offload（默认开，投影 GPU 卸载）、--image-min-tokens` →
  `--mmproj-offload（默认开，投影 GPU 卸载）、--mmproj-device（mmproj 指定设备，留空=跟随 --device，none=不放 GPU）、--image-min-tokens`
- 硬件行（L173）：`取值 auto/none/mmap/mlock/direct_io 等）、--fit（默认开）` →
  `取值 auto/none/mmap/mlock/direct_io 等）、--lazy-mode（on/auto/off，默认 auto，per-layer embeddings 等大张量按行 mmap 按需读盘）、--fit（默认开）`；
  行尾 `--cache-type-k / --cache-type-v（默认 f16）、--n-cpu-moe |` →
  `--cache-type-k / --cache-type-v（默认 f16）、--n-cpu-moe、--n-cpu-ffn（前 N 层 dense FFN 权重留 CPU） |`
- 上下文行（L174）：`--flash-attn（默认 auto）、--swa-full |` →
  `--flash-attn（默认 auto）、--kv-unified-per-slot（每并行 slot 的 context 上限，与 --ctx-size 同设时不生效）、--swa-full |`
- 高级行（L177）：`--perf、--log-prompts-dir` →
  `--perf、--log-jsonl（JSONL 日志，自动禁用彩色）、--log-prompts-dir`

- [ ] **Step 3: 校验残留**

Run: 在主规格文件（`docs/superpowers/specs/2026-07-25-llama-server-launcher-design.md`）内 grep `b10488`
Expected: 仅剩历史实测示例引用（L130/142/300-301/330/341/345/362 一带），无「基线 b10488」残留。（plans/ 下旧计划文档不动。）

- [ ] **Step 4: 提交**

```bash
git add docs/superpowers/specs/2026-07-25-llama-server-launcher-design.md
git commit -m "docs: 主规格基线表述与 §5 参数表更新 b10919"
```

---

### Task 7: 全量验证

- [ ] **Step 1: 类型检查**

Run: `npm run typecheck`
Expected: 无输出、退出码 0。

- [ ] **Step 2: 全量测试**

Run: `npm test`
Expected: 全部套件通过，version/args 新增用例全绿；无 EADDRINUSE（fileParallelism: false 已配置，勿改回并行）。

- [ ] **Step 3: 手动冒烟（可选，用户执行）**

Run: `npm run dev`
- 未装 b10919：选「默认」启动 → 顶栏友好错误「托管基线 b10919 未安装…」；点「立即更新」安装后可正常启动。
- 填 5 个新字段（lazy-mode=on、n-cpu-ffn=2、mmproj-device=none、kv-unified-per-slot=4096、勾 log-jsonl）启动 → 日志无 invalid argument，server 正常加载。
- 版本下拉显示「默认（托管基线 b10919）」。

- [ ] **Step 4: 收尾提交（如有未提交内容）**

```bash
git status
git add -A
git commit -m "chore: 基线 b10919 收尾"
```

（若无未提交内容则跳过本步。）
