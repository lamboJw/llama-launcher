# 可设置数据保存路径 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 config.json / profiles/ / records/ 的保存位置改为可设置（`form.dataDir`），默认 `appRoot/app_data`，启动时一次性迁移旧 `%APPDATA%` 数据，Chromium userData 一并搬走。

**Architecture:** config.json 固定在 `appRoot/app_data/config.json`（AppConfig 构造时传目录）；`form.dataDir` 为空时用默认目录，非空时 `path.resolve`；启动时先 `migrateLegacyData` 再建 store；`app.setPath('userData')` 在 app ready 前同步设置。UI 在 App 组加一个目录字段，走现有 scheduleSave，重启生效。

**Tech Stack:** Electron + TypeScript + vitest（`npm run test` / `npm run typecheck`）

**规格:** `C:\Users\45440\.local\share\opencode\plans\data-dir-design.md`（Task 6 移入 `docs/superpowers/specs/2026-08-26-data-dir-design.md`）

---

### Task 1: FormValues 新增 dataDir 字段

**Files:**
- Modify: `src/shared/types.ts:62-65`（FormValues App 级字段）
- Modify: `src/main/config.ts:42-45`（DEFAULT_FORM App 级字段）
- Test: `test/config.test.ts:36-51`（defaults 测试）

- [ ] **Step 1: 写失败测试**

在 `test/config.test.ts` 的 `defaults: port 8080, ...` 测试（`expect(f.timeout).toBe('');` 之后）加一行：

```ts
    expect(f.dataDir).toBe('');
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/config.test.ts`
Expected: FAIL（`f.dataDir` 为 undefined，且 TS 报错 "Property 'dataDir' does not exist"）

- [ ] **Step 3: 实现**

`src/shared/types.ts` FormValues 末尾 App 级字段改为：

```ts
  // App 级（非 server 参数）
  autoSwitch: boolean; hfCacheDir: string; recordRounds: boolean;
  scanDir: string; exeSelection: string; recordsMaxTotalBytes: number;
  dataDir: string;   // 数据目录；'' = 默认 appRoot/app_data
```

`src/main/config.ts` DEFAULT_FORM 末尾 App 级字段改为：

```ts
  // App 级
  autoSwitch: false, hfCacheDir: DEFAULT_HF_CACHE, recordRounds: false,
  scanDir: '', exeSelection: '', recordsMaxTotalBytes: 1024 * 1024 * 1024,
  dataDir: '',
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/config.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/shared/types.ts src/main/config.ts test/config.test.ts
git commit -m "feat: FormValues 新增 dataDir 字段（默认空=appRoot/app_data）"
```

---

### Task 2: resolveDataDir 纯函数

**Files:**
- Modify: `src/main/config.ts`（新增导出函数）
- Test: `test/config.test.ts`

- [ ] **Step 1: 写失败测试**

在 `test/config.test.ts` 末尾新增（文件头 import 行改为 `import { AppConfig, JsonStore, migrateForm, DEFAULT_FORM, resolveDataDir } from '../src/main/config.js';`）：

```ts
describe('resolveDataDir', () => {
  it('空串/空白 -> 默认目录', () => {
    expect(resolveDataDir('', 'C:\\app\\app_data')).toBe('C:\\app\\app_data');
    expect(resolveDataDir('   ', 'C:\\app\\app_data')).toBe('C:\\app\\app_data');
  });

  it('相对路径 -> path.resolve', () => {
    expect(resolveDataDir('mydata', 'C:\\app\\app_data')).toBe(path.resolve('mydata'));
  });

  it('绝对路径 -> 原样', () => {
    const abs = path.join(os.tmpdir(), 'x');
    expect(resolveDataDir(abs, 'C:\\app\\app_data')).toBe(abs);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/config.test.ts`
Expected: FAIL（`resolveDataDir is not a function` / 导入不存在）

- [ ] **Step 3: 实现**

`src/main/config.ts` 在 `defaultConfigDir()` 之后新增：

```ts
/** dataDir 解析：空 = 默认 app_data 目录；非空 = resolve（相对路径基于 cwd） */
export function resolveDataDir(dataDir: string, appDataDir: string): string {
  const d = dataDir.trim();
  return d === '' ? appDataDir : path.resolve(d);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/config.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/main/config.ts test/config.test.ts
git commit -m "feat: 新增 resolveDataDir（空=默认，非空=resolve）"
```

---

### Task 3: migrateLegacyData 一次性迁移

**Files:**
- Modify: `src/main/config.ts`（新增导出函数）
- Test: `test/config.test.ts`

- [ ] **Step 1: 写失败测试**

在 `test/config.test.ts` 末尾新增（import 行追加 `migrateLegacyData`）：

```ts
describe('migrateLegacyData', () => {
  it('旧目录不存在 -> 无操作', () => {
    const old = path.join(tmpdir(), 'old');
    const nw = tmpdir();
    migrateLegacyData(old, nw);
    expect(fs.existsSync(path.join(nw, 'config.json'))).toBe(false);
  });

  it('源存在目标不存在 -> 复制 config.json 与 profiles/records（源保留）', () => {
    const old = tmpdir();
    fs.writeFileSync(path.join(old, 'config.json'), '{"a":1}');
    fs.mkdirSync(path.join(old, 'profiles'));
    fs.writeFileSync(path.join(old, 'profiles', 'p.json'), '{}');
    fs.mkdirSync(path.join(old, 'records'));
    fs.writeFileSync(path.join(old, 'records', '2026-01-01.jsonl'), 'x\n');
    const nw = tmpdir();
    migrateLegacyData(old, nw);
    expect(fs.readFileSync(path.join(nw, 'config.json'), 'utf8')).toBe('{"a":1}');
    expect(fs.readFileSync(path.join(nw, 'profiles', 'p.json'), 'utf8')).toBe('{}');
    expect(fs.existsSync(path.join(nw, 'records', '2026-01-01.jsonl'))).toBe(true);
    expect(fs.existsSync(path.join(old, 'config.json'))).toBe(true); // 复制不移动
  });

  it('目标已存在 -> 跳过不覆盖（幂等）', () => {
    const old = tmpdir();
    fs.writeFileSync(path.join(old, 'config.json'), 'old');
    const nw = tmpdir();
    fs.writeFileSync(path.join(nw, 'config.json'), 'new');
    migrateLegacyData(old, nw);
    expect(fs.readFileSync(path.join(nw, 'config.json'), 'utf8')).toBe('new');
  });

  it('源单项缺失 -> 跳过该项，其余正常', () => {
    const old = tmpdir();
    fs.writeFileSync(path.join(old, 'config.json'), '{"a":1}');
    const nw = tmpdir();
    migrateLegacyData(old, nw);
    expect(fs.existsSync(path.join(nw, 'config.json'))).toBe(true);
    expect(fs.existsSync(path.join(nw, 'profiles'))).toBe(false);
    expect(fs.existsSync(path.join(nw, 'records'))).toBe(false);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/config.test.ts`
Expected: FAIL（`migrateLegacyData is not a function`）

- [ ] **Step 3: 实现**

`src/main/config.ts` 在 `resolveDataDir` 之后新增：

```ts
/**
 * 旧版数据一次性迁移（%APPDATA%/llama-launcher → 新位置）：
 * 复制不移动；目标已存在即跳过（幂等）；单项失败仅警告不抛异常
 */
export function migrateLegacyData(oldDir: string, newDir: string): void {
  if (oldDir === newDir) return;
  const copyFile = (src: string, dst: string): void => {
    try {
      if (!fs.existsSync(dst) && fs.existsSync(src)) {
        fs.mkdirSync(path.dirname(dst), { recursive: true });
        fs.copyFileSync(src, dst);
      }
    } catch (e) { console.warn(`[migrateLegacyData] 复制 ${src} 失败: ${String(e)}`); }
  };
  const copyDir = (src: string, dst: string): void => {
    try {
      if (!fs.existsSync(dst) && fs.existsSync(src)) fs.cpSync(src, dst, { recursive: true });
    } catch (e) { console.warn(`[migrateLegacyData] 复制 ${src} 失败: ${String(e)}`); }
  };
  copyFile(path.join(oldDir, 'config.json'), path.join(newDir, 'config.json'));
  copyDir(path.join(oldDir, 'profiles'), path.join(newDir, 'profiles'));
  copyDir(path.join(oldDir, 'records'), path.join(newDir, 'records'));
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/config.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/main/config.ts test/config.test.ts
git commit -m "feat: 新增 migrateLegacyData 旧数据一次性迁移（复制、幂等）"
```

---

### Task 4: main 启动顺序重构（config 位置 / dataDir / userData）

**Files:**
- Modify: `src/main/index.ts:7`（import）
- Modify: `src/main/index.ts:24-33`（状态块）
- Modify: `src/main/index.ts:67-68`（删除旧 appRoot/llamaBaseDir 定义，上移）

- [ ] **Step 1: 修改 import**

`src/main/index.ts:7` 改为：

```ts
import { AppConfig, defaultConfigDir, resolveDataDir, migrateLegacyData } from './config.js';
```

- [ ] **Step 2: 重构状态块**

将 `src/main/index.ts` 的 L24-33（`const here = ...` 到 `const recordsDir = ...`）替换为：

```ts
const here = path.dirname(fileURLToPath(import.meta.url));

const appRoot = (): string => (app.isPackaged ? path.dirname(app.getPath('exe')) : process.cwd());
const llamaBaseDir = (): string => path.join(appRoot(), 'llama.cpp');

// ---------- 状态 ----------
let win: BrowserWindow | null = null;
// 数据位置（规格：config.json 固定 appRoot/app_data；profiles/records 跟随 form.dataDir）
const appDataDir = path.join(appRoot(), 'app_data');
migrateLegacyData(defaultConfigDir(), appDataDir);
const config = new AppConfig(appDataDir);
const dataDir = (() => {
  const d = resolveDataDir(config.getSettings().form.dataDir, appDataDir);
  try { fs.mkdirSync(d, { recursive: true }); return d; }
  catch { pushLog(`数据目录 ${d} 不可创建，回退默认 ${appDataDir}`); return appDataDir; }
})();
app.setPath('userData', path.join(dataDir, 'userData'));
const profiles = new ProfilesStore(path.join(dataDir, 'profiles'));
const recordsDir = path.join(dataDir, 'records');
```

然后删除原 L67-68 的旧定义（`const appRoot = ...` 与 `const llamaBaseDir = ...`，现位于 `let updateProgress` 之后）。

注：`pushLog` 是函数声明（hoisted），模块顶层调用安全；`app.setPath` 必须在 app ready 前调用，模块顶层满足。

- [ ] **Step 3: 类型检查**

Run: `npm run typecheck`
Expected: 无错误

- [ ] **Step 4: 手动验证**

Run: `npm run dev`（在 `F:\llama_launcher` 下）
验证：
1. 启动后 `F:\llama_launcher\app_data\config.json` 存在（若 `%APPDATA%\llama-launcher\config.json` 存在则内容一致）
2. `F:\llama_launcher\app_data\userData\` 目录随 Chromium 运行生成
3. 设置里改「数据目录」为其他路径 → 重启 → profiles/records 写入新目录

- [ ] **Step 5: Commit**

```bash
git add src/main/index.ts
git commit -m "feat: 启动时解析 dataDir，config 固定 app_data，userData 随 dataDir"
```

---

### Task 5: UI 新增「数据目录」字段

**Files:**
- Modify: `src/renderer/main.ts:173-174`（buildForm App 组）
- Modify: `src/renderer/main.ts:218`（buildDirField 签名）
- Modify: `src/renderer/main.ts:331-332`（populateForm）

- [ ] **Step 1: 实现**

`src/renderer/main.ts` 三处修改：

1. `buildForm()` App 组，`appHost.appendChild(buildDirField('hfCacheDir', 'HF 缓存目录'));` 之后加：

```ts
  appHost.appendChild(buildDirField('dataDir', '数据目录（重启后生效）'));
```

2. `buildDirField` 签名改为：

```ts
function buildDirField(id: 'scanDir' | 'hfCacheDir' | 'dataDir', label: string): HTMLElement {
```

3. `populateForm()` 中 `set('hfCacheDir', form.hfCacheDir);` 之后加：

```ts
  set('dataDir', form.dataDir);
```

- [ ] **Step 2: 类型检查 + 全量测试**

Run: `npm run typecheck; if ($?) { npm run test }`
Expected: typecheck 无错误；全部测试 PASS

- [ ] **Step 3: Commit**

```bash
git add src/renderer/main.ts
git commit -m "feat: App 组新增「数据目录（重启后生效）」字段"
```

---

### Task 6: 规格文档归档 + 全量验证

**Files:**
- Move: `C:\Users\45440\.local\share\opencode\plans\data-dir-design.md` → `docs/superpowers/specs/2026-08-26-data-dir-design.md`
- Move: `C:\Users\45440\.local\share\opencode\plans\data-dir-plan.md` → `docs/superpowers/plans/2026-08-26-data-dir.md`

- [ ] **Step 1: 移动文档**

```powershell
New-Item -ItemType Directory -Force F:\llama_launcher\docs\superpowers\plans | Out-Null
Move-Item "C:\Users\45440\.local\share\opencode\plans\data-dir-design.md" "F:\llama_launcher\docs\superpowers\specs\2026-08-26-data-dir-design.md"
Move-Item "C:\Users\45440\.local\share\opencode\plans\data-dir-plan.md" "F:\llama_launcher\docs\superpowers\plans\2026-08-26-data-dir.md"
```

并把设计文档第 4 行「状态：已确认（落地时移入…）」改为「状态：已确认」。

- [ ] **Step 2: 全量验证**

Run: `npm run typecheck; if ($?) { npm run test }`
Expected: typecheck 无错误；全部测试 PASS

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/specs/2026-08-26-data-dir-design.md docs/superpowers/plans/2026-08-26-data-dir.md
git commit -m "docs: 数据目录功能规格与实施计划归档"
```
